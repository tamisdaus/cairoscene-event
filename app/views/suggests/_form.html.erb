<%= form_for(suggest) do |f| %>
  <% if suggest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggest.errors.count, "error") %> prohibited this suggest from being saved:</h2>

      <ul>
      <% suggest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
	  <div class="field">
		<%= f.label "Name"%>
		<br>
		<%= f.text_field :sname, class: "form-control"%>
	  </div>

	  <div class="field">
		<%= f.label "Date"%>
		<br>
		<%= f.date_select :sdate, class: "form-control" %>
	  </div>

	  <div class="field">
		<%= f.label "Time" %>
		<br>
		<%= f.text_field :stime, class: "form-control" %>
	  </div>

	  <div class="field">
		<%= f.label "State" %>
		<br>
		<%= f.text_field :splace, class: "form-control" %>
	  </div>

	  <div class="field">
		<%= f.label "City" %>
		<br>
		<%= f.text_field :sarea, class: "form-control" %>
	  </div>

	  <div class="field">
		<%= f.label "Address" %>
		<br>
		<%= f.text_field :saddress, class: "form-control" %>
	  </div>

	  <div class="field">
		<%= f.label "Phone" %>
		<br>
		<%= f.text_field :sphone, class: "form-control" %>
	  </div>

	  <div class="field">
		<%= f.label "Description" %>
		<br>
		<%= f.text_area :sdescription, class: "form-control" %>
	  </div>
		<br>
	  <div class="actions">
		<%= f.submit %>
	  </div>
  </div>
<% end %>
